package Pack;
public class Packages2 {
    public static void main(String[] args){
        Packages P = new Packages();
        P.greet();
    }
}
//we can access the packages as they are public class but compilation can only be done using cmds that were previously used not the og one.